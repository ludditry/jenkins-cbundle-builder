# build mpc, the multiprecision library

./configure CFLAGS="-m32 -I${DESTDIR}/include " CXXFLAGS="-m32 -I${DESTDIR/include} " CPPFLAGS="-fexceptions " --prefix="" --disable-shared --with-gmp=${DESTDIR} --with-mpfr=${DESTDIR}

make
